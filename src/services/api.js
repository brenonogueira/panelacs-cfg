const base_url = process.env.REACT_APP_API_BASEURL;

export const jogador = `${base_url}/jogador`;
export const mapas = `${base_url}/mapas`;
export const funcao = `${base_url}/funcao`;
export const resolucao = `${base_url}/resolucao`;

// export default api
